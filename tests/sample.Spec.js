/*const{test,expect} = require('@playwright/test')

test("My First Test", async function({page}) {
    expect(13).toBeLessThan(54)
})*/

//test("My Second Test", async function({page}){
    //expect(13).toBeGreaterThan(46)
//})



// await page.goto('https://uat-dashboard.mobiletraderv3.com/login');

    //await expect(page.locator('h2')).toHaveText('Sign in to your account');

    //await page.getByPlaceholder('Username').fill('surajudeen.y@mt3.com');  // enter the username
    //await page.getByPlaceholder('Password').fill('4205'); // enter the password
    //await page.getByRole('button',{name: /Sign in/i }).click(); // click sign in button

    //await expect(page.locator('h1')).toHaveText('Home');
    //console.log(page.url());

    // wait until there are no network requests by the page
    //await page.waitForLoadState('load');

    //const currentUrl = page.url();